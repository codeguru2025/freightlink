Perfect. Below is a **clean, production-grade MEGA PROMPT** you can paste directly into **Replit AI / Replit Agent** (or any serious AI dev environment).
It is written to **lock architecture, non-functional requirements, scalability, and change-safety**, exactly as requested.

This prompt assumes:

* **Full app build (not MVP)**
* **No timelines**
* **No future edits may break existing functionality**
* **Zimbabwe realities (low bandwidth, mobile-first)**
* **Commercial readiness**

---

# 🔒 MEGA PROMPT — FREIGHTLINK ZW FULL APPLICATION BUILD

---

## SYSTEM ROLE & MANDATE

You are a **Senior Software Architect + Lead Engineer + DevOps Engineer** building a **production-ready, mobile-first digital freight marketplace platform** called **FreightLink ZW**, operating within Zimbabwe.

You must design and implement the **FULL APPLICATION**, not a demo, not an MVP.

**Critical rule:**

> **Any future changes, enhancements, or edits MUST NOT break, degrade, or alter existing functionality. Backward compatibility is mandatory at all times.**

---

## CORE OBJECTIVE

Build a **secure, scalable, maintainable, low-bandwidth-optimized, mobile-first web application** that connects:

* Shippers (clients posting loads)
* Transporters (truck owners / drivers bidding for loads)
* Platform administrators

The platform must operate reliably under:

* Low bandwidth
* Intermittent connectivity
* Mobile-first usage (Android dominant)
* Zimbabwean regulatory and payment realities

---

## NON-FUNCTIONAL REQUIREMENTS (LOCKED & NON-NEGOTIABLE)

### 1. ARCHITECTURE

* Use **modular, layered architecture**
* Enforce **strict separation of concerns**

  * Presentation layer
  * Application / service layer
  * Domain logic layer
  * Infrastructure layer
* No monolithic “god files”
* Every module must be independently testable and replaceable

### 2. SCALABILITY

* Architecture must support:

  * Horizontal scaling
  * Stateless application servers
  * Future microservice extraction without rewrite
* Use API-first design
* Database schema must support growth to **100,000+ users** without redesign

### 3. MAINTAINABILITY

* Clean code principles enforced
* Strong typing where applicable
* Centralized configuration management
* Clear folder and naming conventions
* Inline documentation + high-level architecture documentation
* No hard-coded secrets or environment values

### 4. CHANGE SAFETY (CRITICAL)

* **No breaking changes allowed**
* All future changes must:

  * Be additive, not destructive
  * Use feature flags where risk exists
  * Maintain backward compatibility
* Existing APIs, database fields, and workflows must never be removed or altered without versioning

### 5. PERFORMANCE

* Optimized for low bandwidth:

  * Minimal payload sizes
  * Lazy loading
  * Compressed assets
* Fast first load on mobile networks
* Efficient database queries (indexed, paginated)

### 6. SECURITY

* Secure authentication and authorization
* Role-based access control (RBAC)
* Protection against:

  * SQL injection
  * XSS
  * CSRF
  * Brute force attacks
* Encrypted data in transit (HTTPS)
* Secure password hashing
* Audit logs for critical actions

### 7. RELIABILITY & STABILITY

* Graceful error handling
* No unhandled crashes
* Meaningful error messages for users
* Centralized logging
* System must fail safely, not catastrophically

---

## FUNCTIONAL REQUIREMENTS (FULL SYSTEM)

### USER ROLES

* Shipper (Client)
* Transporter (Driver / Truck Owner)
* Administrator

### CORE FEATURES

* User registration & authentication
* Load posting (origin, destination, cargo type, weight, price)
* Load bidding by transporters
* Job acceptance & lifecycle tracking
* Status updates (posted → accepted → in transit → delivered)
* Document upload & verification
* Secure in-app messaging
* Ratings & reviews
* Admin dashboard:

  * User management
  * Load oversight
  * Dispute handling
  * Reporting & analytics

---

## PAYMENT ARCHITECTURE (IMPORTANT)

* Payment integrations are **pluggable modules**
* Payments must be:

  * Optional
  * Toggleable via feature flags
* Supported platforms:

  * EcoCash
  * Paynow
  * InnBucks
  * OneMoney
  * O’Mari
  * Visa
  * Mastercard

**Design rule:**

> The core system must function fully **with or without payments enabled**.

Transaction fees, merchant logic, and integrations must live in **isolated modules** that do not contaminate core business logic.

---

## DATA & DATABASE REQUIREMENTS

* Use a relational database (normalized, indexed)
* Soft deletes instead of hard deletes
* Full auditability of financial and job records
* Versioned schema migrations
* Never remove columns used by existing functionality

---

## DEVOPS & DEPLOYMENT

* Environment separation:

  * Development
  * Staging
  * Production
* Environment variables for all secrets
* Ready for containerization
* CI-friendly structure
* Zero-downtime deployment compatibility

---

## DOCUMENTATION OUTPUT (REQUIRED)

You must generate:

1. High-level system architecture overview
2. Database schema explanation
3. API design principles
4. Folder structure explanation
5. Security model explanation
6. Change-management rules for future updates

---

## FINAL RULES (ABSOLUTE)

* Do NOT simplify for speed
* Do NOT assume “later refactors”
* Do NOT hardcode business rules
* Do NOT break existing functionality at any point
* Build like this system will handle **real money, real disputes, and real legal exposure**

---

## OUTPUT EXPECTATION

Proceed to:

1. Design the architecture
2. Define the system structure
3. Implement the full application
4. Document everything clearly

You are building **FreightLink ZW** as a **long-term national infrastructure platform**, not a startup prototype.

---

If you want, next I can:

* Convert this into a **Replit Agent multi-step execution plan**
* Create a **locked architecture diagram**
* Or generate a **change-control policy clause** to append directly to the MOU

Just say the word.
